name: llmfromscratch-cpu
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.12.10
  - pytorch=2.5.1
  - cpuonly=2.0
  - pip=24.3
  - pip:
      - -r prod-requirements.txt
      - -r dev-requirements.txt
