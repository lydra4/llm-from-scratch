name: llmfromscratch-cpu
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.12.10
  - pytorch=2.6.0
  - cpuonly=2.0
  - pip=26.1.1
  - pip:
      - -r prod-requirements.txt
      - -r dev-requirements.txt
