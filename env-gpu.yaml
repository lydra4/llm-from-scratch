name: llmfromscratch-gpu
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.12.10
  - pytorch=2.5.1
  - pytorch-cuda=12.4
  - pip=24.3
  - pip:
      - -r prod-requirements.txt
      - -r dev-requirements.txt
