name: llmfromscratch-gpu
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.12.10
  - pytorch
  - pytorch-cuda
  - pip=24.3
  - pip:
      - -r prod-requirements.txt
      - -r dev-requirements.txt
